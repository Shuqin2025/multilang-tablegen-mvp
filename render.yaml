services:
  - type: web
    name: tablegen-backend
    env: node
    plan: free
    rootDir: backend          # 关键：限制到 backend 子目录
    buildCommand: npm ci
    startCommand: npm start
    healthCheckPath: /api/health
    envVars:
      - key: NODE_VERSION
        value: 18


